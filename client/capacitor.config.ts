import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'ionic-rest-api',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
